import 'package:flutter/material.dart';

class AppColors {
  // OLED Black background
  static const Color background = Color(0xFF000000);
  // Off-white text
  static const Color textPrimary = Color(0xFFF5F5F7);
  // Muted accents – like a piercing blood orange
  static const Color accent = Color(0xFFFF4500); // Blood Orange
  static const Color accentCyan = Color(0xFF00FFFF); // Electric Cyan
  // Crimson red for warnings
  static const Color danger = Color(0xFFDC143C);
  // Matte charcoal for cards
  static const Color surface = Color(0xFF121212);
  // Muted text
  static const Color textSecondary = Color(0xFF9E9E9E);
  // Glass effect overlay (semi-transparent white)
  static const Color glassOverlay = Color(0x1AFFFFFF);
}
